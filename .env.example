# Copy to .env and fill in your key. Never commit .env.
# Note: this app calls the Anthropic API directly from the browser, which
# exposes this key to anyone using the page. Fine for local/internal testing;
# for a real deployment, put a backend proxy in front of the API instead.
VITE_ANTHROPIC_API_KEY=
